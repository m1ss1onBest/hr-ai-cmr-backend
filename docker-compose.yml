services:
  postgres:
    image: postgres:17
    container_name: hrcrm_postgres
    restart: always
    environment:
      POSTGRES_USER: hrcrm
      POSTGRES_PASSWORD: password
      POSTGRES_DB: hrcrm_db 
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: